---
name: 2026-04-04
description: Daily journal - Skill Market集成、wewrite公众号文章流程
type: journal
---

# 2026-04-04

## 今日事项

### Skill Market 集成完成
- ZeroOne Skill Market 安装完成（安装在 `~/.openharness/skills/skill-market`）
- 已为 OpenHarness 和 MyAgents 都装了 Skill Market
- OpenHarness 使用 "openclaw" 框架（openharness 框架不被支持）
- **重要发现**：Skill Market 安装脚本框架参数只支持 claude/codex/cursor/openclaw/opencode/qoder，不支持 openharness

### Skills 安装
- **minimax-skills**：通过 Skill Market 安装，嵌套目录结构问题已解决
  - 解决方案：创建符号链接扁平化
  ```bash
  cd ~/.openharness/skills/minimax-skills
  for dir in */; do name="${dir%/}"; ln -sf "$PWD/$dir/SKILL.md" "../${name}.md"; done
  ```
- **skill-creator**、**humanizer-zh**、**wewrite**、**playwright-skill** 也已安装
- **wechat-article-typeset**：从 Convex URL 直接下载安装

### 微信公众号文章新流程
- **工具组合**：wewrite + wechat-article-typeset + wechat-article-preset-preview
- **流程定义**：
  1. wewrite 生成文章（全自动模式，跳过 Step 2 选题）
  2. 主题按内容风格随机选择
  3. **不上传草稿箱**
  4. 用 wechat-article-typeset 处理生成预览链接
- **文章要求**：
  - 标题下方插入 16:9 手绘简约风格图片
  - 文章末尾不出现相关链接版块
  - 代码块用 \`\`\` 包裹关键指令

### wewrite 排版主题
- wewrite 有自己的主题系统（不同于 wenyan）
- 可用主题：ink-seri, coral-warm, amber-paper, cream-apricot, sakura-soft, teal-fresh, smartblue, mist-blue, ink-bamboo, minimal-bw, dark-calm
- **教训**：之前混用了 wenyan 主题，导致排版不生效

### wechat-article-typeset 预设
- 19 个排版预设
- 暖色系：coral-warm, amber-paper, cream-apricot, sakura-soft
- 科技感：teal-fresh, smartblue, mist-blue
- 墨色系：ink-seri, ink-bamboo（适合深度长文）
- 简约系：minimal-bw, dark-calm

## 技术问题与解决

### OpenHarness Skill 加载限制
- **问题**：`glob("*.md")` 只扫描当前目录，不递归子目录
- **解决**：创建符号链接扁平化嵌套结构

### wewrite CLI 执行问题
- **问题**：Windows asyncio subprocess 限制，直接调用 wewrite CLI 会失败
- **解决**：通过 Agent sub-process 执行 wewrite 脚本

### 微信 API IP 白名单
- **问题**：errcode=40164，IP 不在白名单
- **解决**：用户已将出口 IP 加入白名单

## 工具超时治理方案（2026-04-04）

用户痛点：工具无返回或超时设置不合理不可接受。

**方案：**
1. 创建 `~/.myagents/tools-timeout-reference.md` — 所有工具超时参考
2. 更新 `~/.myagents/CLAUDE.md` — 加入超时规范章节
3. 更新 `04-MEMORY.md` — 写入超时决策

**核心原则：所有工具调用必须显式设置超时，不存在"不设超时"**

**超时错误处理标准：**
```
[超时] 工具名 - 任务描述
预期耗时: Xs
实际耗时: >Xs
原因: (判断)
建议: (重试/拆分/延长)
```

## 待办事项

- [ ] 为公众号文章生成 16:9 手绘简约风格封面图（当前 OpenHarness 无此能力）
- [ ] 探索 wechat-article-preset-preview 的完整功能

## 记忆维护

- 已更新 memory/topics/openharness.md（微信公众号文章发布流程）
- 已更新 04-MEMORY.md（OpenHarness 定位、Skills 系统、工具调用方式）

## 全局技能优化（下午）

### 第一波：描述中文化
- `skill-creator`/`docx`/`download-anything`/`summarize`：描述中文化
- 全局 `hook-assistant` 已是中文，无需修改
- 已提交：`9859211`

### 第二波：精简 Office 技能 body
- `pptx`：483行 → ~70行（移除冗长配色方案）
- `pdf`：294行 → ~70行（精简代码示例）
- `xlsx`：288行 → ~110行（精简金融建模规范）
- 其余技能（paper-interpreter/mermaid/copaw-ops 等）已为中文，无需修改
- 已提交：`508e379`

### 技能加载机制（重要发现！）

MyAgents 技能加载通过 `syncProjectUserConfig` 实现：
- 全局 Skills：`~/.myagents/skills/`（物理位置）
- 启动时 symlink 到：`projects/{name}/.claude/skills/`
- `skills-config.json` 的 `disabled` 控制禁用，不在 `seeded` 列表的不影响加载

**问题**：项目级存在旧版真实目录（非 symlink），覆盖了全局优化版：
- docx(481行) / skill-creator(479行) / pdf(314行) / xlsx(291行) / pptx(232行)
- 全局已优化版：71/167/52/71/49行
- **这些旧目录会先被读到，优化未生效**

**修复**：删除 5 个项目级旧副本，让 symlink 机制生效

### 上下文优化总账
- `~/.myagents/CLAUDE.md`：610行 → 160行（新建 diagnostic-workflow skill）
- `skill-creator`：479行 → 167行
- 净减少约 1,200 行
