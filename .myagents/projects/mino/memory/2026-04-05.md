---
name: 2026-04-05
description: Daily journal - OpenHarness Hindsight MCP修复、记忆整理
type: journal
---

# 2026-04-05

## OpenHarness Hindsight MCP 修复

### 问题
OpenHarness 连接 Hindsight MCP 时报错：
```
RuntimeError: Attempted to exit a cancel scope that isn't the current task's current cancel scope
```

### 根因
anyio 的 `create_task_group()` 与 `Process` 资源管理在 Windows 上有 cancel scope bug。

### 修复方案

**1. stdio_client.py 核心修复**
- 文件：`OpenHarness/.venv/Lib/site-packages/mcp/client/stdio/__init__.py`
- 使用 `asyncio.create_subprocess_exec` 替代 anyio Process
- 移除 `async with (tg, process,)` 资源管理模式
- 任务函数内捕获所有异常防止 task group 损坏

**2. mcp/client.py 错误抑制**
- 文件：`openharness/src/openharness/mcp/client.py`
- 在 `close()` 中抑制 cancel scope RuntimeError

**3. hindsight-stdio.py 响应格式**
- 文件：`~/.openharness/hindsight-stdio.py`
- `tools/call` 结果必须包装为 `CallToolResult` 格式

### 验证结果

| 工具 | 状态 |
|------|------|
| list_banks | ✅ 3个bank |
| get_bank | ✅ |
| get_bank_stats | ✅ 21条记忆 |
| retain | ✅ |
| recall | ✅ |
| reflect | ✅ |
| list_memories | ✅ |
| list_tags | ✅ 9个标签 |

## OpenHarness 记忆整理

### 完成工作

**1. 更新 04-MEMORY.md**
- 添加 4 个话题入口
- 添加 `/update-memory` 命令说明

**2. 新建 topics 文件**
- `openharness-memory.md` — 完整架构文档
- `tool-timeout.md` — 超时规范
- `mcp-config.md` — MCP 配置详情
- `mcp-stdio-fix.md` — 客户端修复详情

**3. Hindsight retain**
- 所有记忆已 retain 到 openharness bank

## OpenHarness 记忆维护机制

**规则**：每次任务完成后提醒用户运行 `/update-memory`

**命令**：`/update-memory`
- 提取会话摘要
- retain 到 Hindsight openharness bank
- 检查 topics 文件状态
- 给出下一步建议

## Hindsight Banks 状态

| Bank | 记忆数 | 标签 |
|------|--------|------|
| openharness | 31 | OpenHarness 任务后通过 /update-memory 存入 |
| default | 173 | MyAgents 项目 |

## GitHub 仓库切换（本日重要决策）

**问题**：HKUDS/OpenHarness（上游）无写权限，MEMORY_UPDATE 推送到该仓库会失败。

**操作**：
1. 移除 HKUDS/OpenHarness remote
2. fork（yiyehuahen/OpenHarness）设为唯一 origin
3. OpenHarness 仓库设为私人（`gh repo edit yiyehuahen/OpenHarness --visibility private`）

**Git 操作**：
- fork 有 divergent history（20+ 上游提交不在本地）
- 用 `git rebase fork/main` 解决后再 force push
- rebase 保留其他贡献者工作，将其变基到本地提交之下

**结果**：
- origin = `https://github.com/yiyehuahen/OpenHarness.git`（私人）
- HKUDS/OpenHarness 已断开
- 所有维护只推送到 yiyehuahen/OpenHarness

## OpenHarness 记忆同时维护

MEMORY_UPDATE 命令（`/UPDATE_MEMORY`）**同时维护两个仓库**：
- mino（白/mino 项目）→ push 到 yiyehuahen/OpenHarness
- OpenHarness → push 到 yiyehuahen/OpenHarness（同一私人仓库）

两个仓库共享同一个 GitHub 私人仓库，但 Hindsight 记忆存在各自独立的 bank：
- mino → `default` bank
- OpenHarness → `openharness` bank

## 更新索引（本次维护）

- ✅ 04-MEMORY.md：关键决策表新增"OpenHarness 仓库：yiyehuahen/OpenHarness（私人fork）"
- ✅ topics/openharness.md：仓库 URL 更新为 yiyehuahen/OpenHarness（私人）
- ✅ Hindsight retain（default bank）：新增 2 条记忆（仓库切换 + rebase 操作）

## Obsidian 知识库建立（下午）

### 完成工作

**1. Obsidian Vault 结构搭建**
- Vault：`C:\Users\Administrator\Documents\Obsidian Vault\`
- CLI wrapper：`~/.local/bin/obsidian`（因为 Obsidian.exe 直接运行有 ICU data 错误）
- 目录：00-知识索引/01-技能卡片/02-项目经验/03-架构决策

**2. 核心文档**
- `00-知识索引/知识入口.md` — vault 总览和使用说明
- `00-知识索引/技能目录.canvas` — 技能地图可视化
- `00-知识索引/工具清单.md` — 完整工具清单（含 OpenHarness）
- `01-技能卡片/白自己的能力档案.md` — 从源码解构自己的功能
- `02-项目经验/项目索引.md` — 项目经验索引
- `03-架构决策/架构决策.md` — 重要技术决策

**3. Obsidian vs Hindsight 功能分工确立**
- Obsidian = 主动整理的知识（技能文档/项目经验/架构决策）
- Hindsight = 自动积累的记忆（决策细节/bug解决/用户偏好）
- 召回流程：04-MEMORY → Obsidian搜索 → Hindsight recall → topics

**4. 自我解构（源码层面）**
- MyAgents 架构：React/Rust/Bun 三层进程模型
- OpenHarness：38工具、30+命令、7内置Skills
- MCP 配置：8个工具（ddg/tavily/github/searxng/playwright/hook/im-cron/feishu）
- 进程模型：Session Sidecar × N、独立 Agent Channel

### 补充文档（晚间）

- `01-技能卡片/微信公众号工作流.md` — wewrite流程 + 排版主题 + API上传
- `03-架构决策/Docker服务运维.md` — Hindsight持久化修复 + 诊断命令 + IM格式
- `00-知识索引/工具清单.md` 更新：新增 Hook 系统章节

### Hindsight retain

- 4条新记忆已 retain 到 default bank

## 定时知识库维护

- 已设置每日 23:55 定时任务（im-cron）
- 交付渠道：微信
- 每次新建 Session 执行完整维护流程