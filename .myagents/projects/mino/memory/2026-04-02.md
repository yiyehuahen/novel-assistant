---
name: 2026-04-02
description: Daily journal - session start
type: journal
---

# 2026-04-02

## 今日事项

- Mino 身份探索：了解了自己的身世（OpenMino 模板 / GitHub: hAcKlyc/mino）
- 深入源码：研究 MyAgents 上下文压缩机制（100K token 阈值、compact boundary）
- 研究不同渠道的上下文共享机制

## 重要发现

### 上下文压缩机制（MyAgents SDK）
- 阈值：100,000 tokens（硬编码 aH）
- 可配置项：contextTokenThreshold（每个 Agent 可改）
- 压缩后：历史消息替换为 <summary> 结构化文本，详细历史存 JSONL 文件
- 压缩提示："If you need specific details from before compaction, read the full transcript at: [文件路径]"

### 渠道上下文隔离
- 所有渠道共用同一 AgentDir → 文件记忆共享
- 每个渠道独立 Session → 对话上下文隔离
- sessions.json 中 source 字段标识渠道来源（feishu_private / openclaw-weixin_private）

### Memory Auto-Update
- 读取 UPDATE_MEMORY.md 作为 prompt
- 让 AI 自己总结并更新 04-MEMORY.md、memory/ 日志
- 完成后做 git commit

## 用户反馈

- **微信渠道只支持纯文本，不支持 Markdown** → 回复时避免使用 Markdown 格式
- **命令行超时问题**：Windows Git Bash 与 Docker 路径冲突，导致 exec 命令无响应。教训：避免在 bash 中直接用 docker exec，改用 Python 脚本

## 晚间工作：Hindsight 记忆服务配置

### 问题排查
- MiniMax API (api.minimaxi.com) 不支持 response_format json_object，Hindsight 验证失败
- 尝试用 hsk_ (硅基流动) API 启动 Hindsight 成功

### 最终状态
- Hindsight 容器运行在 **host 网络模式**
- 健康检查通过：`{"status":"healthy","database":"connected"}`
- MiniMax API 端点：`https://api.minimaxi.com/v1`，模型：`abab6.5s-chat`

### API 路径（已验证）
- 健康检查：`/health`
- OpenAPI 文档：`/openapi.json`
- 记忆操作：`/v1/default/banks/{bank_id}/memories` (POST retain, GET recall)
- 格式：`{"items": [{"content": "..."}]}`

### 待完成
- 端口8888在Windows宿主机访问失败（docker exec 正常），需进一步排查网络
